flask==2.3.3
flask-session==0.5.0
flask-limiter>=3.5.0
requests==2.31.0
werkzeug==2.3.7
python-dotenv==1.0.0
urllib3==2.0.7
certifi==2023.7.22
openai>=1.0.0

# PayPal integration dependencies
# requests - Already included above for HTTP requests to PayPal API
# base64 - Built-in Python module for encoding credentials
# json - Built-in Python module for API data handling

# Production-ready session storage
redis>=4.5.0
flask-redis>=0.4.0

# Enhanced testing capabilities
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.11.0
pytest-asyncio>=0.21.0

# Additional utility packages for production
psutil>=5.9.0
croniter>=1.4.0
cryptography>=41.0.0
email-validator
certbot

# Database dependencies
# sqlite3 - Built-in SQLite support (Python 3.x standard library)
# Note: All required database dependencies are built-in to Python:
# - sqlite3: SQLite database interface
# - threading: Thread-based parallelism
# - queue: Thread-safe queue implementation
# - concurrent.futures: High-level threading interface
# - contextlib: Context management utilities
# - pathlib: Object-oriented filesystem paths
# - uuid: UUID generation
# - datetime: Date and time handling
# - json: JSON encoder/decoder (for configuration only, not data persistence)
# - logging: Logging facility
# - os: Operating system interface
# - time: Time-related functions

# Email system dependencies (built-in with Python 3.6+)
# smtplib - Built-in SMTP client
# email.mime - Built-in MIME message construction